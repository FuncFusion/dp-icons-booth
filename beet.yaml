output: "build"

meta:
  bolt:
    entrypoint:
      "dp_icons:main"

require:
  - bolt
  - bolt_expressions

data_pack:
  load: 
    - src
    - summit-dp*.zip

resource_pack:
  load:
    - src
    - summit-rp*.zip

pipeline:
  - mecha
